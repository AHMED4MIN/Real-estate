import Link from "next/link";
import { agents } from "./data";

export default function AgentsPage() {
  return <main className="min-h-screen bg-surface text-on-surface"><header className="border-b border-outline-variant/40 bg-surface"><nav className="mx-auto flex h-20 max-w-container-max items-center justify-between px-margin-desktop"><Link href="/" className="font-headline-md text-headline-md font-bold text-primary">EstateFlow</Link><Link href="/" className="font-semibold text-primary">Back to home</Link></nav></header><section className="bg-surface-container-low py-16"><div className="mx-auto max-w-container-max px-margin-desktop"><p className="text-label-bold font-label-bold uppercase text-secondary">Local expertise</p><h1 className="mt-3 max-w-2xl font-display-lg text-display-lg">Find an agent who understands your move.</h1><p className="mt-5 max-w-xl text-body-lg text-on-surface-variant">Meet experienced local professionals, learn what they specialize in, and contact the right person for your next step.</p></div></section><section className="mx-auto max-w-container-max px-margin-desktop py-20"><div className="grid gap-gutter sm:grid-cols-2 lg:grid-cols-4">{agents.map((agent) => <Link key={agent.slug} href={`/agents/${agent.slug}`} className="property-card-shadow overflow-hidden rounded-xl bg-white"><img src={agent.image} alt={agent.name} className="h-64 w-full object-cover" /><div className="p-stack-md"><p className="text-label-bold font-label-bold uppercase text-secondary">{agent.city}</p><h2 className="mt-2 font-headline-md text-headline-md">{agent.name}</h2><p className="mt-1 text-sm font-semibold text-primary">{agent.title}</p><p className="mt-4 text-body-sm text-on-surface-variant">{agent.bio}</p><span className="mt-5 inline-block font-bold text-primary">View profile →</span></div></Link>)}</div></section></main>;
}
