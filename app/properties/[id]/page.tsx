import Link from "next/link";
import { notFound } from "next/navigation";
import { formatPrice, propertyFacts, Agent, Property } from "@/app/lib/properties";
import { isSupabaseConfigured, supabase } from "@/app/lib/supabase";

export const dynamic = "force-dynamic";
const facilityIcons: Record<string, string> = { school: "🎓", supermarket: "🛒", mosque: "🕌", hospital: "⚕", pharmacy: "💊", park: "🌳", "public transport": "🚌", restaurant: "🍽" };
function externalUrl(value: string | null) { try { const url = new URL(value ?? ""); return ["https:", "http:"].includes(url.protocol) ? url.toString() : null; } catch { return null; } }

export default async function PropertyPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  if (!isSupabaseConfigured || !supabase) notFound();
  const { data } = await supabase.from("properties").select("*").eq("id", id).eq("published", true).single();
  const property = data as Property | null;
  if (!property) notFound();
  const { data: agentData } = property.agent_id ? await supabase.from("agents").select("*").eq("id", property.agent_id).eq("published", true).single() : { data: null };
  const agent = agentData as Agent | null;
  const images = [...new Set([property.image_url, ...(property.gallery ?? [])].filter((image): image is string => Boolean(image)))];
  const support = property.country_support ? new Intl.NumberFormat("en-US").format(property.country_support) : null;
  const instagramVideoUrl = externalUrl(property.instagram_video_url);

  return <main className="min-h-screen bg-surface text-on-surface"><header className="border-b border-outline-variant/40"><nav className="mx-auto flex h-20 max-w-container-max items-center justify-between px-margin-desktop"><Link href="/" className="font-headline-md text-headline-md font-bold text-primary">al salah</Link><Link href={property.listing_type === "sale" ? "/buy" : `/${property.listing_type}`} className="font-semibold text-primary">← All properties</Link></nav></header><section className="mx-auto max-w-container-max px-margin-desktop py-12"><p className="text-label-bold font-label-bold uppercase text-secondary">{property.listing_type} · {property.property_type}</p><h1 className="mt-3 font-display-lg text-display-lg">{formatPrice(property)}</h1><p className="mt-2 text-body-lg text-on-surface-variant">{property.address}</p><div className="mt-8 grid gap-4 md:grid-cols-2">{images.map((image, index) => <img key={image} src={image} alt={`${property.address} ${index + 1}`} className={`h-72 w-full rounded-xl object-cover ${index === 0 ? "md:row-span-2 md:h-full" : ""}`} />)}</div>{property.video_url && <section className="mt-12"><h2 className="font-headline-lg text-headline-lg">Property video</h2><video controls preload="metadata" poster={property.image_url ?? undefined} className="mt-5 w-full rounded-xl bg-black" src={property.video_url}>Your browser does not support property videos.</video></section>}<div className="mt-12 grid gap-10 lg:grid-cols-[2fr_1fr]"><div><h2 className="font-headline-lg text-headline-lg">Property details</h2><p className="mt-5 whitespace-pre-line text-body-lg text-on-surface-variant">{property.description || property.details || "More details will be added soon."}</p>{instagramVideoUrl && <a href={instagramVideoUrl} target="_blank" rel="noreferrer" className="mt-8 flex items-center justify-between gap-4 rounded-2xl border border-pink-300 bg-gradient-to-r from-pink-50 via-white to-orange-50 p-5 transition hover:-translate-y-0.5 hover:shadow-md"><span><span className="text-xs font-bold uppercase tracking-widest text-pink-700">Property video</span><span className="mt-1 block font-headline-md text-headline-md">Watch the full tour on Instagram</span><span className="mt-1 block text-sm text-on-surface-variant">See every room, detail, and the surrounding area.</span></span><span aria-hidden className="grid size-11 shrink-0 place-items-center rounded-full bg-gradient-to-br from-fuchsia-600 via-pink-500 to-orange-400 text-xl text-white">▶</span></a>}{support && <section className="mt-10 rounded-2xl border border-primary/20 bg-primary-container/30 p-6"><p className="font-bold text-primary">Eligible for country support</p><p className="mt-1 text-on-surface-variant">This property may receive <strong>{support} DH</strong> in government housing support.</p></section>}{(property.facilities?.length ?? 0) > 0 && <section className="mt-10"><p className="text-label-bold font-label-bold uppercase text-secondary">Around the property</p><h2 className="mt-2 font-headline-lg text-headline-lg">Nearby facilities</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{property.facilities?.map((facility) => <div key={facility} className="flex items-center gap-3 rounded-xl border bg-surface-container-low p-4"><span className="grid size-10 place-items-center rounded-full bg-primary-container text-lg">{facilityIcons[facility] ?? "📍"}</span><span className="font-semibold capitalize">{facility}</span></div>)}</div></section>}</div><aside className="h-fit rounded-xl bg-surface-container-low p-6"><p className="text-sm text-outline">Features</p><p className="mt-3 font-headline-md text-headline-md">{propertyFacts(property)}</p><p className="mt-4 text-on-surface-variant">{property.city}</p>{agent && <div className="mt-6 border-t border-outline-variant/40 pt-5"><p className="font-bold">Contact agent</p><p className="mt-2 text-sm font-semibold">{agent.name}</p>{agent.phone && <p className="mt-1 text-sm text-on-surface-variant">{agent.phone}</p>}</div>}</aside></div></section></main>;
}
